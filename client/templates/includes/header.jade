//
   Created by osi on 16. 3. 15.

template(name='header')
  nav.navbar.navbar-default(role='navigation')
    .container-fluid
      .navbar-header
        button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#navigation')
          span.sr-only Toggle navigation
          span.icon-bar
          span.icon-bar
          span.icon-bar
        a.navbar-brand(href="{{pathFor 'postList'}}") Microscope
      #navigation.collapse.navbar-collapse
        ul.nav.navbar-nav
          // currentUser : accounts package 에서 제공하는 helper 임
          if currentUser
            li
              a(href="{{pathFor 'postSubmit'}}") Submit Post
        ul.nav.navbar-nav.navbar-right
          +loginButtons